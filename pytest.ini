[pytest]
addopts = -q
testpaths =
    tests
markers =
    integration: tests that run against real EEG data in data/
    slow: slow tests
filterwarnings =
    ignore:distutils Version classes are deprecated:DeprecationWarning
